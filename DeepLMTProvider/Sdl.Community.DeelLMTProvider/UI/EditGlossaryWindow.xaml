<Window x:Class="Sdl.Community.DeepLMTProvider.UI.EditGlossaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sdl.Community.DeepLMTProvider.UI"
        xmlns:controls="clr-namespace:Sdl.Community.DeepLMTProvider.UI.Controls"
        xmlns:uiHelpers="clr-namespace:Sdl.Community.DeepLMTProvider.UI.UiHelpers"
        mc:Ignorable="d"
        MaxWidth="600"
        MaxHeight="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Title="Edit glossary"
        WindowStyle="None"
        SizeToContent="WidthAndHeight"
        d:DataContext="{d:DesignInstance Type=local:EditGlossaryWindow, IsDesignTimeCreatable=True}"
        d:DesignHeight="450"
        d:DesignWidth="800">

    <Window.InputBindings>
        <KeyBinding Key="E"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Edit"
                    Modifiers="Control" />

        <KeyBinding Key="Escape"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Escape" />

        <KeyBinding Key="Delete"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Delete" />

        <KeyBinding Key="Enter"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Enter" />

        <KeyBinding Key="Up"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Up" />

        <KeyBinding Key="Down"
                    Command="{Binding KeyboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="Down" />
    </Window.InputBindings>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/buttonresources.xaml" />
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/datagridresources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="DataGridCell_Style"
                   TargetType="DataGridCell">
                <Setter Property="Focusable"
                        Value="False" />
            </Style>

            <Style TargetType="Button"
                   BasedOn="{StaticResource Sdl.Button.EmbeddedStyle}">
                <Setter Property="Focusable"
                        Value="False" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <uiHelpers:WindowsControl  DockPanel.Dock="Top"
                                   Grid.Row="0" />

        <DataGrid Grid.Row="1"
                  Focusable="False"
                  Name="Entries_DataGrid"
                  SelectionMode="Single"
                  AutoGenerateColumns="False"
                  d:ItemsSource="{d:SampleData}"
                  CellStyle="{StaticResource DataGridCell_Style}"
                  Style="{StaticResource Sdl.DataGrid.GenericStyle}"
                  IsReadOnly="False"
                  ItemsSource="{Binding GlossaryEntries, RelativeSource={RelativeSource AncestorType=Window}}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="*"
                                        Header="Source terms">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:EditableTextBlock Text="{Binding SourceTerm, Mode=TwoWay}"
                                                        IsEditing="{Binding IsEditing, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="*"
                                        Header="Target terms">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:EditableTextBlock Text="{Binding TargetTerm, Mode=TwoWay}"
                                                        IsEditing="{Binding IsEditing, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="❌"
                                    Command="{Binding DeleteEntryCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="2"
                   LastChildFill="False">
            <Button DockPanel.Dock="Right"
                    Margin="0 5"
                    IsDefault="True"
                    Focusable="False"
                    Click="ApplyButton_Click"
                    Style="{StaticResource Sdl.Button.PrimaryStyle}"
                    Content="Apply" />
            <Button DockPanel.Dock="Left"
                    Margin="0 5"
                    Click="AddRowButton_Click"
                    ToolTip="Add new entry"
                    IsDefault="True"
                    Content="➕" />
            <Button Margin="0 5"
                    Name="Edit_Button"
                    DockPanel.Dock="Left"
                    Content="📝"
                    Click="EditButton_Click"
                    ToolTip="Edit glossary"
                    IsDefault="True" />
        </DockPanel>
    </Grid>
</Window>