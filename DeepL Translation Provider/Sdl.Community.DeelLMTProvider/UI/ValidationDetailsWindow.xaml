<Window x:Class="Sdl.Community.DeepLMTProvider.UI.ValidationDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Language Pair Validation Details"
        Width="600"
        Height="420"
        MinWidth="400"
        MinHeight="250"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="CanResize"
        ShowInTaskbar="False">

    <Grid Margin="14,12,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 x:Name="MessageTextBox"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 FontFamily="Segoe UI"
                 FontSize="12"
                 Background="#F8F8F8"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Padding="8"
                 VerticalAlignment="Stretch" />

        <Button Grid.Row="1"
                Content="Close"
                HorizontalAlignment="Right"
                Margin="0,10,0,0"
                Padding="20,5"
                IsCancel="True"
                Click="Close_Click" />
    </Grid>
</Window>
