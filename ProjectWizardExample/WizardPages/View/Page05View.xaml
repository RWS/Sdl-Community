<UserControl x:Class="ProjectWizardExample.WizardPages.View.Page05View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="200" Width="400">
    <Grid Margin="0 50 0 0">
		<TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="16" Text="{Binding Path=DisplayName, FallbackValue='Page Name'}" Height="25" Margin="0,0,0,0"/>
		<CheckBox Content="Data is valid?" HorizontalAlignment="Center" VerticalAlignment="Top" IsChecked="{Binding Path=IsValid, Mode=TwoWay}" Height="20" Margin="0,30,0,0"/>
		<CheckBox Content="Wizard completed successfully?" HorizontalAlignment="Center" IsChecked="{Binding Path=IsComplete, Mode=TwoWay}" Margin="0,55,0,0"/>
    </Grid>
</UserControl>
