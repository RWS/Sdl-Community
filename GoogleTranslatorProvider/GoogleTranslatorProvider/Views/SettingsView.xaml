<UserControl x:Class="GoogleTranslatorProvider.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:root="clr-namespace:GoogleTranslatorProvider"
             xmlns:uiHelpers="clr-namespace:GoogleTranslatorProvider.UiHelpers"
             xmlns:viewModels="clr-namespace:GoogleTranslatorProvider.ViewModels"
             xmlns:converters="clr-namespace:GoogleTranslatorProvider.Converters"
             d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="800">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Styles/Buttons.xaml"/>
				<ResourceDictionary Source="../Styles/GroupBox.xaml" />
				<ResourceDictionary Source="../UiHelpers/WatermarkTextBoxResources.xaml" />
				<ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/TextBlockResources.xaml"/>
				<ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/Checkboxresources.xaml"/>
				<ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/ButtonResources.xaml"/>
				<ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/TextBoxResources.xaml"/>
			</ResourceDictionary.MergedDictionaries>
			<converters:InvertableBoolEnabledConverter x:Key="InvertableBoolEnabledConverter"/>
			<converters:InvertableBooleanToVisibilityConverter x:Key="InvertableBooleanToVisibilityConverter"/>
		</ResourceDictionary>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<GroupBox Header="Settings"
				  Style="{StaticResource DefaultGroupBox}"
				  Grid.Row="1"
				  Margin="24 13 24 0"
				  Padding="0 12 0 12">

			<StackPanel Margin="24 0 24 0">
				<CheckBox Content="{x:Static root:PluginResources.ReSendDescription}"
					  Style="{StaticResource Sdl.Checkbox.GenericStyle}"
					  IsChecked="{Binding ReSendDraft}"
					  Margin="0 0 0 12"/>

				<CheckBox Content="{x:Static root:PluginResources.SendPlainDescription}"
					  Style="{StaticResource Sdl.Checkbox.GenericStyle}"
					  IsChecked="{Binding SendPlainText}"
					  Margin="0 0 0 12"/>

				<CheckBox Content="{x:Static root:PluginResources.PreLookDescription}"
					  Style="{StaticResource Sdl.Checkbox.GenericStyle}"
					  IsChecked="{Binding DoPreLookup}"
					  Margin="0 0 0 0"/>

				<StackPanel Margin="0 4 0 0"
							Orientation="Horizontal"
						   Visibility="{Binding DoPreLookup, Converter={StaticResource InvertableBooleanToVisibilityConverter}}">
					<TextBox Text="{Binding PreLookupFileName}"
							 Style="{StaticResource WatermarkTextBox}"
							 uiHelpers:TextBoxWatermarkHelper.WatermarkText="{x:Static root:PluginResources.PreLookupWaterMark}"
							 uiHelpers:TextBoxWatermarkHelper.ButtonCommand="{Binding ClearCommand}"
							 uiHelpers:TextBoxWatermarkHelper.ButtonCommandParameter="PreLookupFileName"
							 IsEnabled="{Binding DoPreLookup,Converter={StaticResource InvertableBoolEnabledConverter}}"
							 VerticalContentAlignment="Center"
							 Width="300"
							 Margin="0 0 10 0"/>
					<Button Content="{x:Static root:PluginResources.BrowseBtn}"
							Style="{StaticResource Sdl.Button.DialogStyle}"
							Command="{Binding BrowseCommand}"
							CommandParameter="{x:Static root:PluginResources.PreLookBrowse}"
							IsEnabled="{Binding DoPreLookup,Converter={StaticResource InvertableBoolEnabledConverter}}"
							DockPanel.Dock="Left"
							Cursor="Hand"
							Width="80"/>
				</StackPanel>

				<CheckBox Content="{x:Static root:PluginResources.PostLookupDescription}"
						  Style="{StaticResource Sdl.Checkbox.GenericStyle}"
						  IsChecked="{Binding DoPostLookup}"
						  Margin="0 12 0 4"/>
				
				<StackPanel Visibility="{Binding DoPostLookup, Converter={StaticResource InvertableBooleanToVisibilityConverter}}"
							Orientation="Horizontal">
					<TextBox Text="{Binding PostLookupFileName}"
							 Style="{StaticResource WatermarkTextBox}"
							 uiHelpers:TextBoxWatermarkHelper.WatermarkText="{x:Static root:PluginResources.PostLookupWaterMark}"
							 uiHelpers:TextBoxWatermarkHelper.ButtonCommand="{Binding ClearCommand}"
							 uiHelpers:TextBoxWatermarkHelper.ButtonCommandParameter="PostLookupFileName"
							 IsEnabled="{Binding DoPostLookup,Converter={StaticResource InvertableBoolEnabledConverter}}"
							 VerticalContentAlignment="Center"
							 Width="300"
							 Margin="0 0 10 0"/>
					<Button Content="{x:Static root:PluginResources.BrowseBtn}"
							Style="{StaticResource Sdl.Button.DialogStyle}"
							Command="{Binding BrowseCommand}"
							CommandParameter="{x:Static root:PluginResources.PostLookupBrowse}"
							IsEnabled="{Binding DoPostLookup,Converter={StaticResource InvertableBoolEnabledConverter}}"
							DockPanel.Dock="Right"
							Cursor="Hand"
							Width="80"/>
				</StackPanel>
				
			</StackPanel>
		</GroupBox>

		<StackPanel Grid.Row="2">
			<TextBlock Text="{Binding ErrorMessage, UpdateSourceTrigger=PropertyChanged}"
					   Style="{StaticResource Sdl.TextBlock.ErrorTitleStyle}"
					   TextWrapping="WrapWithOverflow"/>
		</StackPanel>
	</Grid>
</UserControl>