<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:watermark="clr-namespace:Sdl.Community.MTCloud.Provider.UiHelpers.Watermark">
	<Style x:Key="WatermarkPasswordBox" TargetType="PasswordBox">
		<Setter Property="BorderBrush" Value="#999999"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="Margin" Value="0 0 0 10"/>
		<Setter Property="Width" Value="400"/>
		<Setter Property="HorizontalAlignment" Value="Left"/>
		<Setter Property="Visibility" Value="Visible"/>
		<Setter Property="IsEnabled" Value="True"/>
		<Setter Property="Cursor" Value="IBeam"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="2"
                            Padding="3"
                            SnapsToDevicePixels="True">
						<DockPanel>
							<ScrollViewer x:Name="PART_ContentHost"
                                          Margin="4 0 0 0"/>
							<TextBlock x:Name="Watermark" x:Uid="Watermark"
                                       Text="{TemplateBinding watermark:PasswordBoxWatermarkHelper.WatermarkText}"
                                       Foreground="#AF000000"
                                       VerticalAlignment="Center"
                                       FontStyle="Italic"/>
						</DockPanel>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="watermark:PasswordBoxWatermarkHelper.WatermarkIsVisible" Value="True">
							<Setter TargetName="Watermark"
                                    Property="Visibility" Value="Visible"/>
						</Trigger>
						<Trigger Property="watermark:PasswordBoxWatermarkHelper.WatermarkIsVisible" Value="False">
							<Setter TargetName="Watermark"
                                    Property="Visibility" Value="Collapsed"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>