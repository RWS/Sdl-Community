<UserControl x:Class="Sdl.Community.PostEdit.Versions.HTMLReportIntegration.ReportView.Controls.ReportSegmentFilter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedItems"
                              Source="{Binding Items}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Group" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>


    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListView x:Name="AvailableFilterItems"
                  ItemsSource="{Binding Source={StaticResource GroupedItems}}"
                  Grid.Column="0">
        </ListView>

        <StackPanel Grid.Column="1">
            <ListView x:Name="SelectedFilterItems">


            </ListView>

            <GroupBox Header="Relationship">

                <StackPanel>
                    <TextBlock Text="Operator: "/>
                    <ComboBox>
                        <ComboBoxItem Content="AND"/>
                        <ComboBoxItem Content="OR"/>
                    </ComboBox>
                </StackPanel>

            </GroupBox>

        </StackPanel>
    </Grid>





</UserControl>
