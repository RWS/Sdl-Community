<UserControl x:Class="QATracker.BatchTasks.UI.VerifyFilesExtendedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QATracker.BatchTasks.UI"
             Name="Root"
             DataContext="{Binding ElementName=Root}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/TextBlockResources.xaml" />
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/checkboxresources.xaml" />
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/groupboxresources.xaml" />
                <ResourceDictionary Source="/Sdl.Desktop.Platform.Styles;component/buttonresources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="CheckBox"
                   BasedOn="{StaticResource Sdl.Checkbox.GenericStyle}">
                <Setter Property="Margin"
                        Value="3" />
            </Style>

            <Style TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Margin"
                        Value="3" />
                <Setter Property="Width"
                        Value="120" />
                <Setter Property="Height"
                        Value="25" />
                <Setter Property="HorizontalAlignment"
                        Value="Right" />
                <Setter Property="VerticalAlignment"
                        Value="Bottom" />
                <Setter Property="BorderBrush"
                        Value="#adadad" />

            </Style>

            <Style TargetType="StackPanel">
                <Setter Property="Margin"
                        Value="10" />
            </Style>

            <Style TargetType="GroupBox"
                   BasedOn="{StaticResource Sdl.GroupBox.SingleLineStyle}">
                <Setter Property="Margin"
                        Value="0" />
            </Style>
            <SolidColorBrush x:Key="Button.MouseOver.Background"
                             Color="#e5f1fb" />
            <SolidColorBrush x:Key="Button.MouseOver.Border"
                             Color="#0078d7" />
            <SolidColorBrush x:Key="Button.Pressed.Background"
                             Color="#cce4f7" />
            <SolidColorBrush x:Key="Button.Pressed.Border"
                             Color="#005499" />
            <SolidColorBrush x:Key="Button.Disabled.Background"
                             Color="#FFF4F4F4" />
            <SolidColorBrush x:Key="Button.Disabled.Border"
                             Color="#FFADB2B5" />
            <SolidColorBrush x:Key="Button.Disabled.Foreground"
                             Color="#FF838383" />
            <ControlTemplate x:Key="ButtonTemplate1"
                             TargetType="{x:Type ButtonBase}">
                <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                    <ContentPresenter x:Name="contentPresenter"
                                      Focusable="False"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      Margin="{TemplateBinding Padding}"
                                      RecognizesAccessKey="True"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="Button.IsDefaulted"
                             Value="true">
                        <Setter Property="BorderBrush"
                                TargetName="border"
                                Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                    </Trigger>
                    <Trigger Property="IsMouseOver"
                             Value="true">
                        <Setter Property="Background"
                                TargetName="border"
                                Value="{StaticResource Button.MouseOver.Background}" />
                        <Setter Property="BorderBrush"
                                TargetName="border"
                                Value="{StaticResource Button.MouseOver.Border}" />
                    </Trigger>
                    <Trigger Property="IsPressed"
                             Value="true">
                        <Setter Property="Background"
                                TargetName="border"
                                Value="{StaticResource Button.Pressed.Background}" />
                        <Setter Property="BorderBrush"
                                TargetName="border"
                                Value="{StaticResource Button.Pressed.Border}" />
                    </Trigger>
                    <Trigger Property="IsEnabled"
                             Value="false">
                        <Setter Property="Background"
                                TargetName="border"
                                Value="{StaticResource Button.Disabled.Background}" />
                        <Setter Property="BorderBrush"
                                TargetName="border"
                                Value="{StaticResource Button.Disabled.Border}" />
                        <Setter Property="TextElement.Foreground"
                                TargetName="contentPresenter"
                                Value="{StaticResource Button.Disabled.Foreground}" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource Sdl.TextBlock.InformationTitleStyle}"
                   Text="Verify Files Extended" />
        <GroupBox Grid.Row="1"
                  Header="General">
            <StackPanel>
                <CheckBox Content="Report ignored messages" />
                <CheckBox Content="Include verification details" />
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="2"
                  Header="Report segments with the following statuses">
            <DockPanel>
                <ItemsControl Name="Statuses"
                              Margin="10"
                              ItemsSource="{Binding AllStatuses}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding }"
                                      Checked="StatusChecked"
                                      Unchecked="StatusUnchecked" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Template="{DynamicResource ButtonTemplate1}"
                        Name="AllStatusesButton"
                        Content="Check/uncheck all"
                        Click="AllStatusesButton_OnClick" />
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
