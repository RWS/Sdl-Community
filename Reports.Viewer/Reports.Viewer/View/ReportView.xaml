<UserControl x:Class="Sdl.Community.Reports.Viewer.View.ReportView"             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
             xmlns:converters="clr-namespace:Sdl.Community.Reports.Viewer.Converters"
             xmlns:viewModel="clr-namespace:Sdl.Community.Reports.Viewer.ViewModel"
             xmlns:view="clr-namespace:Sdl.Community.Reports.Viewer.View"
             mc:Ignorable="d" 			 
             d:DesignHeight="150" d:DesignWidth="650" d:DataContext="{d:DesignInstance viewModel:ReportViewModel}">
	<UserControl.Resources>
		<ResourceDictionary>
			
			<converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" TrueValue="Visible" FalseValue="Hidden"/>
		</ResourceDictionary>

	</UserControl.Resources>

	<Border Margin="5,5,5,15">
		<Grid Margin="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Border Grid.Column="0" Grid.Row="0" Margin="0">
				<ContentControl Content="{Binding Browser}"/>

				<!--<Border.Style>
					<Style TargetType="{x:Type Border}">
						<Style.Triggers>

							<DataTrigger Binding="{Binding IsTest, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
								<Setter Property="Background">
									<Setter.Value>
										<LinearGradientBrush>
											<LinearGradientBrush.GradientStops>
												<GradientStop Color="Aquamarine" Offset="0"/>
												<GradientStop Color="Beige" Offset="0.2"/>
												<GradientStop Color="Firebrick" Offset="0.5"/>
												<GradientStop Color="DarkMagenta" Offset="0.9"/>
											</LinearGradientBrush.GradientStops>
										</LinearGradientBrush>
									</Setter.Value>
								</Setter>
							</DataTrigger>
							
						</Style.Triggers>
					</Style>
				</Border.Style>-->
			</Border>

		</Grid>
	</Border>
</UserControl>
