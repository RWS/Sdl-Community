<UserControl x:Class="Sdl.Community.SdlTmAnonymizer.View.Translations"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Sdl.Community.SdlTmAnonymizer.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="..\Styles\DataGridStyle.xaml"/>
				<ResourceDictionary Source="..\Styles\ButtonsStyle.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid Height="{Binding ElementName=ParentControl}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<CheckBox  Grid.Row="0" FontFamily="Segoe UI" Margin="5,5,0,0" IsChecked="{Binding SelectAll}" Command="{Binding SelectAllCommand}">Select all</CheckBox>

		<controls:SortAwareDataGrid Grid.Row="1"
		                DefaultColumnName="Name"
		                DefaultSortDirection="Ascending"
						Style="{StaticResource FilesGridStyle}"
						FontFamily="Segoe UI"
						BorderThickness="0" 
						VerticalScrollBarVisibility="Auto"
						HorizontalScrollBarVisibility="Disabled"
						ItemsSource="{Binding RulesCollection, Mode=TwoWay}"  
						Background="White" 
						GridLinesVisibility="None" 
						CanUserAddRows="True"
		                SelectionMode="Extended"
		                SelectionUnit="FullRow"
						SelectedItem="{Binding SelectedItem}"
						SelectedItemsList="{Binding SelectedItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
						MaxHeight="{Binding Path=ActualHeight, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}"
						Margin="5,10" >
				<DataGrid.InputBindings>
					<KeyBinding Key="Delete" Command="{Binding RemoveRuleCommand}"/>
				</DataGrid.InputBindings>
				<DataGrid.Columns>
				<DataGridTemplateColumn Header="Preview" Width="Auto" IsReadOnly="True" MinWidth="50" SortMemberPath="IsSelected">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				<DataGridTextColumn Width="2*" Header="Rule" Binding="{Binding Name}" IsReadOnly="False" ElementStyle="{StaticResource WrapText}" />
				<DataGridTextColumn Width="3*" Header="Description" Binding="{Binding Description}" IsReadOnly="False" ElementStyle="{StaticResource WrapText}"/>
			</DataGrid.Columns>
			</controls:SortAwareDataGrid>
		
		<StackPanel Grid.Column="0" Grid.Row="2"  Orientation="Vertical"  Margin="5,15,10,10">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" >
				<Button  Style="{DynamicResource LightGreenButtonsStyle }" Width="125"  Command="{Binding ImportCommand}">
					<StackPanel Orientation="Horizontal">
						<!--<fa:FontAwesome Icon="ArrowDown" Margin="0,2,5,0"/>-->
						<TextBlock>Import rules</TextBlock>
					</StackPanel>
				</Button>
				<Button  Style="{DynamicResource LightGreenButtonsStyle }" Width="125"  Command="{Binding ExportCommand}">
					<StackPanel Orientation="Horizontal">
						<!--<fa:FontAwesome Icon="ArrowUp" Margin="0,2,5,0"/>-->
						<TextBlock>Export rules</TextBlock>
					</StackPanel>
				</Button>
				<Button  Style="{DynamicResource StudioGreenButtonsStyle }" Width="125"  Command="{Binding PreviewCommand}">
					<StackPanel Orientation="Horizontal">
						<!--<fa:FontAwesome Icon="Eye" Margin="0,0,5,0"/>-->
						<TextBlock>Preview</TextBlock>
					</StackPanel>
				</Button>
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>







