<UserControl x:Class="Sdl.Community.TmAnonymizer.Ui.TranslationMemory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sdl.Community.TmAnonymizer.Ui"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0">Please select tm(s)</TextBlock>
	    <ToolBar Grid.Row="1">
			<Button Command="{Binding SelectFoldersCommand}">Select folders</Button>
		    <Button>Select TM files</Button>
			<Button>Remove selected tms</Button>
	    </ToolBar>
	</Grid>
</UserControl>
