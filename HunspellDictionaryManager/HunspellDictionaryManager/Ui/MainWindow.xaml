<controls:MetroWindow x:Class="Sdl.Community.HunspellDictionaryManager.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sdl.Community.HunspellDictionaryManager.Ui"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
		GlowBrush="#008C95"
        WindowTitleBrush="#008C95"
        Title="HunspellDictionaryManager" Height="450" Width="800"
		ResizeMode="NoResize"
		WindowStartupLocation="CenterScreen">
	<Window.Resources>
		<ResourceDictionary >
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../UiHelpers/ButtonsStyle.xaml"/>
				<ResourceDictionary Source="../UiHelpers/TextBlockStyle.xaml"/>
				<ResourceDictionary Source="../UiHelpers/TextBoxStyle.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>
	<Grid>
		<Grid.Resources>
			<Style TargetType="{x:Type TextBox}">
				<Setter Property="Margin" Value="10,5,5,5"/>
				<Setter Property="Height" Value="25"/>
				<Setter Property="Validation.ErrorTemplate" Value="{DynamicResource ValidationErrorTemplate}" />
			</Style>
			<Style TargetType="{x:Type Label}">
				<Setter Property="Foreground" Value="#3EA691"/>
			</Style>			
		</Grid.Resources>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"></RowDefinition>
			<RowDefinition Height="auto"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"></ColumnDefinition>
			<ColumnDefinition Width="*"></ColumnDefinition>
			<ColumnDefinition Width="*"></ColumnDefinition>
		</Grid.ColumnDefinitions>
		<Label Name="selectLbl" Grid.Row="0"  Grid.Column="0" Content="Please select appropiate dictionary language" Margin="10 0 0 0" />
		<ComboBox Grid.Row="0" Grid.Column="1" Height="25"
			ItemsSource="{Binding DictionaryLanguages, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				         SelectedItem="{Binding SelectedDictionaryLanguage}"  DisplayMemberPath ="DisplayName" SelectedIndex="0"
		    controls:TextBoxHelper.ClearTextButton="True"
			controls:TextBoxHelper.Watermark="Choose alternative dictionary language">			
		</ComboBox>
		<TextBox  Grid.Row="0" Grid.Column="2"
				 controls:TextBoxHelper.Watermark="Enter new dictionary language"  ToolTip="Dictionary name format example: de_De or de-DE" 
		         Text="{Binding NewDictionaryLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
		</TextBox>
		<Button Style="{DynamicResource StudioGreenButtonsStyle}" Command="{Binding CreateHunspellDictionaryCommand}" Content="Create" Grid.Row="2" Grid.Column="2" Width="100" Height="30"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 10 0 0"/>
	</Grid>
</controls:MetroWindow>