trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/HowToEditTargetText/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/HowToEditTargetText.sln'
  pluginName: 'Code samples\HowToEditTargetText\HowToEditTargetText'
  artifactName: 'HowToEditTargetText.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml