trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Community Wpf Helpers/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.CommunityWpfHelpers.sln'
  pluginName: 'CommunityWpfHelpers/Sdl.CommunityWpfHelpers'
  artifactName: 'Sdl.CommunityWpfHelpers.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
