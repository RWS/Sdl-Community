trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Word Cloud/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.WordCloud.sln'
  pluginName: 'Word Cloud/Sdl.Community.WordCloud.Plugin'
  artifactName: 'Sdl.Community.WordCloud.Plugin.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml