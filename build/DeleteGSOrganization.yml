trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/DeleteGSOrganization/*

pr:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/DeleteGSOrganization/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/DeleteGSOrganization.sln'

steps:
  - template: jobs/build-sdlplugin.yml
