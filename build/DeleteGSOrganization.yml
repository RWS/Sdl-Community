trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/DeleteGSOrganization/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/DeleteGSOrganization.sln'
  pluginName: 'Code samples\DeleteGSOrganization\DeleteGSOrganization'
  artifactName: 'DeleteGSOrganization.sdlplugin'

steps:
  - template: jobs/build-sdlplugin-without-PublishBuildArtifacts-task.yml
