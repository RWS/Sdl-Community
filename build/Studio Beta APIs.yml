trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Studio Beta APIs/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/SDL.Community.BetaAPIs.sln'
  pluginName: 'TAUS Data Search/Sdl.Community.Taus.Translation.Provider'
  artifactName: 'Sdl.Community.Taus.Translation.Provider.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
 # - template: jobs/PublishBuildArtifact-task.yml