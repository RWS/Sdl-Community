trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/AddTUToTM/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/AddTUToTM.sln'
  pluginName: 'AddTUToTM/AddTUToTM'
  artifactName: 'Sdl.Community.AddTUToTM.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
