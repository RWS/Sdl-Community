trigger:
  branches:
    include:
     - studio_2019
  paths:
    include:
     - TuToTm/*

pr:
  branches:
    include:
     - studio_2019
  paths:
    include:
     - TuToTm/*

pool: 
  vmImage : '$(DefaultBuildVm)'

variables:
- template: jobs/branch-variables.yml
- name: buildPlatform
  value: 'Any CPU'
- name: buildConfiguration
  value: 'Release'
- name: solution 
  value: '**/Sdl.Community.TuToTm.sln'
- name: pathToPublish
  value: 'TuToTm/TuToTm\bin\Release\Sdl.Community.TuToTm.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/publish-build-artifact-task.yml