trigger:
  branches:
    include:
     - Studio_2026
  paths:
    include:
     - Trados Number Verifier/*

pr:
  branches:
    include:
     - Studio_2026
  paths:
    include:
     - Trados Number Verifier/*

variables: 
- group: 'NumberVerifierSonarVersion'
- template: jobs/branch-variables.yml
- name: buildPlatform 
  value: 'Any CPU'
- name: buildConfiguration
  value: 'Release'
- name: pathToPublish
  value: '$(PluginPath)\Sdl.Community.NumberVerifier.sdlplugin'
- name: solution
  value: '**/Sdl.Community.NumberVerifier.sln'
- name : SonarID
  value : Trados-Number_Verifier
- name : SonarName
  value : 'Trados Number Verifier'
- name : ProjectVersion
  value : $[counter(variables['Version.Patch'], 1)]

pool:
  vmImage: '$(DefaultBuildVm)'

steps:
  - task: SonarCloudPrepare@3
    inputs:
      SonarCloud: 'AppStorePlugins-ServiceConnection'
      organization: 'sdl'
      scannerMode: 'dotnet'
      projectKey: $(SonarId)
      projectName: $(SonarName)
      projectVersion: $(ProjectVersion)

  - template: jobs/build-sdlplugin.yml
  - task: SonarCloudAnalyze@3
    displayName: 'Run Code Analysis'

  - task: SonarCloudPublish@3
    inputs:
      pollingTimeoutSec: '300'
    displayName: 'Publish Quality Gate Result'

  - template: jobs/publish-build-artifact-task.yml
