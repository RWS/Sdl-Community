trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Antidote Verifier/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.AntidoteVerifier.sln'
  pluginName: 'Antidote Verifier\Sdl.Community.AntidoteVerifier\Sdl.Community.AntidoteVerifier'
  artifactName: 'Sdl.Community.AntidoteVerifier.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/PublishBuildArtifact-task.yml