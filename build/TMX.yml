trigger:
  branches:
    include:
     - master
  paths:
    include:
     - TMX/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.FileTypeSupport.TMX.sln'
  pluginName: 'TQA/TQA'
  artifactName: 'Sdl.Community.TQA.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
 # - template: jobs/PublishBuildArtifact-task.yml