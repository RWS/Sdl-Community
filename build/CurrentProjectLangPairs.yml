trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/CurrentProjectLangPairs/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/CurrentProjectLangPairs.sln'
  pluginName: 'Code samples\CurrentProjectLangPairs\CurrentProjectLangPairs'
  artifactName: 'CurrentProjectLangPairs.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml