trigger:
  branches:
    include:
     - master
  paths:
    include:
     - ApplyTMTemplate/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.ApplyTMTemplate.sln'
  pluginName: 'ApplyTMTemplate'
  artifactName: 'Sdl.Community.ApplyTMTemplate.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
