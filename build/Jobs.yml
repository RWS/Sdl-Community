trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Jobs/*

pr:
  branches:
    include:
     - master
  paths:
    include:
     - Jobs/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.Jobs.sln'
  pluginName: 'Jobs\Sdl.Community.Jobs'
  artifactName: 'Sdl.Community.Jobs.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/PublishBuildArtifact-task.yml