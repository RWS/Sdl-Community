trigger:
  branches:
    include:
     - master
  paths:
    include:
     - YourProductivity/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.YourProductivity.sln'
  pluginName: 'YourProductivity/Sdl.Community.YourProductivity'
  artifactName: 'Sdl.Community.YourProductivity.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml