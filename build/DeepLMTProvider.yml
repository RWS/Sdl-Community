trigger:
  branches:
    include:
     - master
  paths:
    include:
     - DeepLMTProvider/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.DeepLMTProvider.sln'
  pluginName: 'DeepLMTProvider\Sdl.Community.DeelLMTProvider'
  artifactName: 'Sdl.Community.DeepLMTProvider.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/PublishBuildArtifact-task.yml
