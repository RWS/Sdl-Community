trigger:
  branches:
    include:
     - master
  paths:
    include:
     - CleanUpTasks/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.CleanUpTasks.sln'
  pluginName: 'CleanUpTasks/CleanUpTasks'
  artifactName: 'SDLCommunityCleanUpTasks.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/PublishBuildArtifact-task.yml
