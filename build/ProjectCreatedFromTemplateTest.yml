trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/ProjectCreatedFromTemplateTest/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.ProjectCreatedFromTemplateTest.sln'
  pluginName: 'Code samples\ProjectCreatedFromTemplateTest\Sdl.Community.ProjectCreatedFromTemplateTest'
  artifactName: 'Sdl.Community.ProjectCreatedFromTemplateTest.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
