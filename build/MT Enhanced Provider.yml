trigger:
  branches:
    include:
     - master
  paths:
    include:
     - MT Enhanced Provider/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.MtEnhancedProvider.sln'
  pluginName: 'MT Enhanced Provider\Sdl.Community.MtEnhancedProvider'
  artifactName: 'Sdl.Community.MtEnhancedProvider.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/PublishBuildArtifact01-task.yml