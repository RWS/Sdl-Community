trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/EditScript/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/EditScript.sln'
  pluginName: 'Code samples\EditScript\EditScript'
  artifactName: 'EditScript.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
