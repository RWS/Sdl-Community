trigger:
  branches:
    include:
     - master
  paths:
    include:
     - FileTypeSupport.MXLIFF/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.FileTypeSupport.MXLIFF.sln'
  pluginName: 'AdvancedDisplayFilter'
  artifactName: 'Sdl.Community.AdvancedDisplayFilter.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
