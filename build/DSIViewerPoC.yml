trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/DSIViewerPoC/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/DSIViewerPoC.sln'
  pluginName: 'Code samples\DSIViewerPoC\DSIViewerPoC'
  artifactName: 'DSIViewerPoC.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
