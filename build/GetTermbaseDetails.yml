trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/GetTermbaseDetails/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/GetTermbaseDetails.sln'
  pluginName: 'Code samples\GetTermbaseDetails\GetTermbaseDetails'
  artifactName: 'GetTermbaseDetails.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml