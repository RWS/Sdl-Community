trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/GetTermbaseDetails/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/GetTermbaseDetails.sln'

steps:
  - template: jobs/build-sdlplugin-without-PublishBuildArtifacts-task.yml