trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/MultiTermStandAloneTestApp/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/MultiTermStandAloneTestApp.sln'

steps:
  - template: jobs/build-sdlplugin-without-PublishBuildArtifacts-task.yml