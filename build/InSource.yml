trigger:
  branches:
    include:
     - Studio_2024
  paths:
    include:
     - Trados InSource!/*

pr:
  branches:
    include:
     - Studio_2024
  paths:
    include:
     - Trados InSource!/*

pool: 
  vmImage : '$(DefaultBuildVm)'

variables:
- template: jobs/branch-variables.yml
- name: buildPlatform
  value: 'Any CPU'
- name: buildConfiguration
  value: 'Release'
- name: solution
  value : '**/Sdl.Community.InSource!.sln'
- name: pathToPublish
  value: '$(PluginPath)\Sdl.Community.InSource.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
  - template: jobs/publish-build-artifact-task.yml
