trigger:
  branches:
    include:
     - master
  paths:
    include:
     - ETS Translation Provider/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/ETSLanguageResourceBuilder.sln'
  pluginName: 'ETSLanguageResourceBuilder/Sdl.Community.ETSLanguageResourceBuilder'
  artifactName: 'Sdl.Community.ETSLanguageResourceBuilder.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
