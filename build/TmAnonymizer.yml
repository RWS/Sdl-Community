trigger:
  branches:
    include:
     - master
  paths:
    include:
     - TmAnonymizer/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.SdlTmAnonymizer.sln'
  pluginName: 'Sdl.Community.SdlTmAnonymizer'
  artifactName: 'Sdl.Community.SdlTmAnonymizer.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
 # - template: jobs/PublishBuildArtifact-task.yml