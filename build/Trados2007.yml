trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Trados2007/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Trados2007.sln'
  pluginName: 'Trados2007/Trados2007'
  artifactName: 'Sdl.Community.Trados2007.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml