trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/ProjectController/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/ProjectController.sln'
  pluginName: 'Code samples\ProjectController\ProjectController'
  artifactName: 'ProjectController.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
