trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/Create ServerTm/*

pr:
  branches:
    include:
     - master
  paths:
    include:
     - Code samples/Create ServerTm/*

pool: Studio-OX

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Create ServerTm.sln'
  pluginName: 'Code samples\Create ServerTm\Create ServerTm'
  artifactName: 'Create ServerTm.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml
